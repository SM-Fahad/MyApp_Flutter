import 'package:flutter/material.dart';

class bag extends StatelessWidget {
  const bag({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Bag", style: TextStyle(fontSize: 30)));
  }
}
